{
  "name": "synology-ddns-helper",
  "version": "2.1.0",
  "description": "A Node.js-based script to integrate DDNS update with Synology NAS, Cloudflare updater with a modern TypeScript implementation.",
  "main": "dist/cloudflare/main.js",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "build:bundle": "esbuild src/cloudflare/main.ts src/cloudflare/php.ts src/cloudflare/template.ts --bundle --platform=node --target=node18 --format=cjs --outdir=dist/cloudflare --log-level=warning",
    "build": "pnpm run clean && pnpm run typecheck && pnpm run build:bundle",
    "verify-dist": "pnpm run build && git diff --exit-code dist/"
  },
  "keywords": [
    "synology",
    "ddns",
    "cloudflare",
    "nas"
  ],
  "author": "NavyStack",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.0"
  }
}
